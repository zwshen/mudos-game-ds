
	rename(3)		MudOS v21c2		(5 Sep 1994)

	名稱:
		rename() - 更改一個檔案的名稱.
	語法:
		int rename( string src, string dst );

		整數 rename( 字串 src, 字串 dst );
	用法:
		把叫做 src 的檔案改名叫做 dst.
	傳回值:
		改名成功就傳回 0.  失敗則傳回非零值.
	參考:
		rm(3), rmdir(3), cp(3), link(3)
	翻譯:
		Spock @ FF	97.May.27.		(printed 3/16/95)
