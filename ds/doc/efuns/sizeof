
	sizeof(3)		MudOS v21c2		(5 Sep 1994)

	名稱:
		sizeof() - 傳回一個陣列 (array)、映射 (mapping)、
			   暫存區 (buffer)、或是字串 (string) 的
			   元素數目.
	語法:
		int sizeof( mixed var );

		整數 sizeof( 混合 var );
	用法:
		傳回 var 中元素的數目, var 可以是陣列、陣列、字串、
		或是暫存區. 如果 var 不屬於以上四種型態, 就傳回 0.
	參考:
		allocate(3), allocate_mapping(3), strlen(3)
	翻譯:
		Spock @ FF	97. Jul.23.		(printed 3/16/95)
