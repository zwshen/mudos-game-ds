
	new(3)			MudOS v21c2		(5 Sep 1994)

	名稱:
		new() - 載入並複製一個物件.
	語法:
		object new( string name );

		物件 new( 字串 name );
	用法;
		以 name 定義的檔案名稱載入一個新的物件, 並且給此物件一個
		獨特的名稱. 此函式傳回這個新的物件. 一個 environment()
		為非零值的物件無法複製. 
	參考:
		clone_object(3), destruct(3), move_object(3)
	翻譯:
		Spock @ FF	97.Apr.24.		(printed 3/16/95)
