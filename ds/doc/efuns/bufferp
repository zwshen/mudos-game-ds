
	bufferp(3)		MudOS v21c2		(5 Sep 1994)

	名稱:
		bufferp() - 看一個給定的變數是否為一個暫存區 (buffer).
	語法:
		int bufferp( mixed arg );

		整數 bufferp( 混合 arg );
	用法:
		如果 arg 是一個暫存區就傳回 1, 不是就傳回 0.
	參考:
		mapp(3), stringp(3), pointerp(3), objectp(3), intp(3),
		bufferp(3), floatp(3), functionp(3), nullp(3),
		undefinedp(3), errorp(3)
	翻譯:
		Spock @ FF   96.Oct.16.			(printed 3/16/95)
