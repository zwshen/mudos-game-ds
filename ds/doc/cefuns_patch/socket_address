     socket_address(3)      MudOS (5 Sep 1994)       socket_address(3)

     名稱：
          socket_address() - 傳回一個 efun socket 的遠端位址

     語法：
          #include <socket_err.h>

          string socket_address( int s );

          字串 socket_address( 整數 s );

     用法：
          socket_address() 傳回 efun socket s 的遠端位址。傳回的位
          址格式如下：

               "127.0.0.1 23".

     傳回值：
          socket_address() 傳回：

               成功時是一個字串形式的位址。

               失敗時是一個空字串。

     參照：
          socket_connect(3), socket_create(3), resolve(3),
          query_host_name(3), query_ip_name(3), query_ip_number(3)

     翻譯：
          Kenny@Broken.History       97.Jul.27       (printed 3/16/95)
