// Room: /u/s/sevil/1f/16.c

inherit ROOM;

void create()
{
	set("short", "寬廣的走道");
	set("long", @LONG
這裡是寬廣的走道，兩旁的商場讓你看的目不暇給，不知道該往哪邊走才
LONG
	set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"11.c",
  "north" : __DIR__"21.c",
  "west" : __DIR__"17.c",
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
