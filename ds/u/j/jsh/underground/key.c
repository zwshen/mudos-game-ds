#include <ansi.h>
inherit ITEM;

void create()
{
        set_name(HIW"白色指骨"NOR,({"white finger","white","finger"}));
        set_weight(50);
        set("unit", "根");
        set("long","一根白色的骨頭, 前端微勾, 有點像鑰匙 .. ?\n");
        set("door","whitefinger");
        set("value",100);
}

