//updated
inherit ITEM;
void create()
{
        set_name("飼料", ({ "food" }) );
        set_weight(20);
                set("unit", "包");
                set("long",
                        "一包飼料，上頭印著動物園的LOGO。\n"
                        "可以拿來餵食小動物。 \n");
                set("value", 100);
        
}