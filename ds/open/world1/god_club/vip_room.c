#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIC"真"HIY"‧"HIC"淳子"NOR);
        set("long", @long

沒想到,.. 被你看到了，這裡就是 Tear 聖主的私人房間，你可以
在這裡做很多事，例如 hammer tear, kick tear 就是幾項不錯的
主意。 :D :D

long    );
        set("light", 1);
        set("exits", ([
  "southwestdown": __DIR__"ladder-1.c",
]));
        setup();
        set("stroom", 1);
}
