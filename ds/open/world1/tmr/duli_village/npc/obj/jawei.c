inherit ITEM;

void create()
{
        set_name("紅色寶石", ({"red jewel","jewel","_OBJ_ID_QUEST_JAWEI_"}));
        set("long",@long
這是一顆純潔血紅的寶石，約有拳頭大小，散發出來的紅色光芒非常耀眼。
long
        );
        set("unit", "顆");
        set("weight",100);
        set("value",5000);
        setup();
}

