inherit ROOM;

void create()
{
	set("short", "禮房");
	set("long", @LONG
不好意思．．．．看不懂。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"path9",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
