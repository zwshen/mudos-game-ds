
	keys(3)			LP MudOSv21c7		(7 Jul 1996)

	函數名稱:
		keys() - 傳回在一個映射變數 (mapping) 中所有
			 (關鍵字, 內容值) (即 (key, value) ) 資料
			 關鍵字的陣列 (array).
	函數語法:
		mixed *keys( mapping m );

		混合 *keys( 映射 m );
	函數語法:
		keys()  照順序傳回一個在映射變數 m  中 (關鍵字, 內容值) 
		之所有關鍵字的陣列.

		□例:

		mapping m;
		m = (["hp" : 35, "sp" : 42, "mass" : 100]);

		則:

		keys(m) == ({"hp", "sp", "mass"})

		請注意: 關鍵字不會排序. 而與 values() 傳回內容值的
			原來順序相同.

	函數用法:
		values(3), each(3)
	手冊翻譯:
		Phoebus@ZH	97.Feb.18.		(Writen 5/23/97)
