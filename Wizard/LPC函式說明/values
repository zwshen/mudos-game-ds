
	values(3)		LP MudOSv21c7		(7 Jul 1996)

	函數名稱:
		values() - 從一個映射變數的 (關鍵字, 內容值) 
			   ( (keys, values) )  中, 以陣列傳回內容值.
	函數語法:
		mixed *values( mapping m );

		混合 *values( 映射 m );
	函數用法:
		values() 傳回一個陣列. 此陣列為映射變數 m  中,  
		(關鍵字, 內容值) 的所有內容值元素.

		□例:

			mapping m;

			m = (["hp" : 35, "sp" : 42, "mass" : 100]);

		則

			values(m) == ({35, 42, 100})

		請注意, 傳回的內容值陣列, 其元素順序與關鍵字的順序一樣.
	參考函數:
		keys(3), each(3)
	手冊翻譯:
		Phoebus@ZH	97.jul.25.		(Writen 5/23/97)
